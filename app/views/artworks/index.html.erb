<% @artworks.each do |artwork| %>
  <div class="artwork">
    <h2><%= artwork.title %></h2>
    <p><%= artwork.description %></p>
  </div>
  <% end %>
<%= cl_image_tag("",
      width: 400, height: 300, crop: :fill) %>
